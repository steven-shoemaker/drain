[package]
name = "drain-core"
version = "0.1.0"
edition = "2021"
description = "State machine, prompts, and parsing for Drain"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
serde_json = "1"
